{
  "name": "voicebox-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "format": "prettier --write .",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "typescript": "^5.8.2",
    "vitest": "^4.0.18",
    "wrangler": "^4.0.0"
  }
}
